какие коллекции должны быть в моём языке и как они должны обрабатываться?
  N вопросов на которые должен ответить создатель языка
более удобный switch(patter matching???)
массивы
списки
Языки должен быть прост, давать всё легко но и позволять отключить и делать низкоуровневое
нужно ли стандартное выносить в стд модули?
раскрутка 
начать с мин асм, возможность расширять асмом своей вм. с вм начать проще.

http://www.wikihow.com/Create-a-Programming-Language
http://www.drdobbs.com/architecture-and-design/so-you-want-to-write-your-own-language/240165488

Синтаксис:
  : вместо = - удобнее печатать но надо удобнее читать, поэтому лучше длиннее! может ->
  ? в именах предикатах null?
  автоматически понимать где : и не должно быть его, но можно поставить чтобы в одну строку записать
    def a(): ret 0
    def b()
        ret 1
  - вместо _  snake_case_style or snake-case-style 
  минимум () {} [] по возможности использовать одни . , > < ; : #$%^&*
= лучше : его лучше видно. а ещё лучше слово зарезервировать? define 

понравилось в перле отдельное сравнение для строк и чисел
хорошая идея из пхп это ресурс, отдельный тип для внешних штук
  переменные переменных могли бы быть полезны в виде свойств и замене шаблонов?
    $obj->$field
    хотя если объекты позволяют индексировать как хеши то по строке тоже самое будет
    будет ли адекватно позволить обращаться просто если нету данного имени поля то искать
во внешней области переменную с данным именем. хотя лучше явно это делать. можно
опечататься и будет незаметно.
в питоне лучший repl
Нужно чтобы всё, вся стандартная библиотека, структуры и алгоритмы были основой ассемблера
машины. то есть даже спец инструкции для словарей, списоков, поиска. такая абстрактная
машина не низкоуровневая. точнее этот уровень уже станет низким(средним) для языка уже
точно высокого уровня. возможно даже паттерны, модули, классы, функции.
Для простоты реализцаии эволюционных алгоритмов данного языка стоит сделать альтернативный
синтаксис(или основной) простой, (как дерево, лисп) возможно односимвольный(брейнфак).

пакеты явы и именование.
